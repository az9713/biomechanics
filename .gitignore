# Claude Code — personal, per-project local settings (never commit)
.claude/settings.local.json
# Subagent worktrees (isolated repo copies — never commit)
.claude/worktrees/
# Python bytecode from the vendored skill's scripts (regenerated on run)
.claude/skills/**/__pycache__/
*.pyc

# Scratch / temp — not for the repo (previews stay in scratchpad per CLAUDE.md)
.ignore/
here.sh.txt
module08-preview.png

# Local transcript backups (sync-transcripts skill)
claude-transcripts/

# Personal reference images dropped in the repo (not part of the course)
human_template.JPEG
elbow.jpg
